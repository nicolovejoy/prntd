# SDD ledger — plan: docs/superpowers/plans/2026-09-08-paper-leftovers.md

Worktree: /Users/nico/src/prntd/.claude/worktrees/paper-leftovers
Branch: feat/paper-leftovers
Merge base: a653f67
Plan commit: 71e2398

## Pre-flight conflict scan

Spec: the controller brief, reproduced verbatim in the plan's §"Spec, verbatim".
It is reachable, so rulings are not provisional.

### Pairwise (tasks sharing a file or an interface)

| A | B | Shared | A produces | B consumes | Finding |
| --- | --- | --- | --- | --- | --- |
| T1 | T2 | none | picker label classes | — | No shared file. Clean. |
| T1 | T3 | `docs/design-system.md` (T3 only) | mono picker labels | T3 step 5 describes `SizePicker`/`ColorPicker` under "Composites" | Ordering is correct: T1 lands before T3, so T3 describes the post-change state. No conflict. |
| T1 | T4 | none | — | — | Clean. |
| T2 | T3 | none | — | — | Clean. |
| T2 | T4 | `docs/design-system.md` (T4 only) | `ORDERS` masthead, square `/admin/published` card | T4 step 6 explicitly describes both | Ordering correct (T2 before T4). T4 step 6 instructs verification in the source file, so it describes its own branch's state. No conflict. |
| T3 | T4 | `docs/design-system.md` | corrected Parts 1–2 vocabulary (Studio=/studio, Shop, retired surfaces) | T4 must use the same words in Part 3 | Same file, DISJOINT ranges: T3 is bounded to lines 1–508 ("do not touch anything at or after `## Part 3`"), T4 is bounded to Part 3 onward. Sequential execution, no merge conflict. T4's Interfaces block names the dependency. Clean. |

### Per-task self-consistency

| Task | Checked | Finding |
| --- | --- | --- |
| T1 | Test snippets vs. the file they target | Verified against the real `src/components/__tests__/product-options.test.tsx`: `COLORS` exists at line 5 with the shape the new test uses; both `describe` blocks exist; `render`/`screen`/`fireEvent`/`vi` already imported. `not.toContain("text-sm")` is safe — the new class string contains `text-[11px]` and `text-text-muted`, neither of which contains the substring `text-sm`. Clean. |
| T1 | Files created vs. files later touched | Creates nothing. Modifies two files, neither touched by T2–T4. Clean. |
| T2 | Test snippet vs. `src/app/orders/__tests__/orders-list.test.tsx` | Verified: the file imports `render`, `screen`, `fireEvent`, has a `makeOrder` factory, and `OrdersList` renders its `h1` unconditionally (the empty state only replaces the list below), so `render(<OrdersList orders={[]} />)` reaches the heading. Clean. |
| T2 | The two edits against each other | `/orders` and `/admin/published` are unrelated files in one commit. Justified: both are the same one-shot "match the Paper treatment already shipped next door" change, and neither carries its own test cycle beyond one assertion. Clean. |
| T3 | Its own instructions vs. the Global Constraints | Constraint "no price ever" vs. step 7's grep for `From \$`/`19.43` — mutually reinforcing, and the plan's own prose never writes a price. Constraint "do not add new sections" vs. step 5's "add the mono label role if it is missing" — a role line inside an existing bullet list is a correction, not a section; R8 states this. Clean. |
| T4 | Steps vs. R8/R9 | Step 2 deletes the `HomeHero` item and renumbers; R8 forbids restructuring but permits correcting a false claim, and a file that does not exist is a false claim. R9 governs `/order`, `/designs`, `/prints`, `/dashboard`, `/shop/[slug]` — all five appear in the steps with "keep the heading, one line saying retired". Consistent. Clean. |
| T4 | The one thing that must not move | `/` Home item 6 (the Pricing rule) is named as frozen in the Method block, in step 2, and in the Global Constraints. Three mentions, no contradiction. Clean. |

No conflicts found. No pre-flight rulings required. Proceeding to Task 1.

## Progress

Ruling: Tasks 1 and 2 dispatched as ONE batch to a single sonnet implementer (two commits) — both are the same shape (swap a class string to a Paper treatment that already ships next door), the brief carries the complete code, and the combined diff is ~35 lines with four new assertions. One review seat instead of two. Cost if wrong: a defect in one task's diff is reviewed alongside the other's, slightly diluting reviewer attention over ~35 lines. Cheap.
Task 1+2: dispatched (sonnet, batched), BASE 71e2398
Deferred (out of fence, found while gathering doc facts): `src/components/published-grid.tsx` docblock lines 19-21 still say the card shows "then what it costs and on what garment" — #226 deleted that price line. A future sweep reading the docblock would re-add a price. Report in the PR body for a slice that owns that file.
Deferred (out of fence): `src/app/cart/page.tsx:97` still renders `<h1 className="text-xl sm:text-2xl font-bold mb-6">Your cart</h1>` — the same pre-Paper display heading this slice removes from /orders. PR #221 re-laid /cart's rows but left the masthead. After this slice, /shop and /orders carry mono mastheads and /cart does not. Report in the PR body.
Task 1+2: implementer DONE, commits b91822b (mono picker labels) + 393f3fd (ORDERS masthead, square moderation card). 1736/1736 tests, lint 0 errors, typecheck clean. Review package review-71e2398..393f3fd.diff; task reviewer dispatched (sonnet).
Ruling: Task 3's implementer is NOT dispatched in parallel with the Tasks 1+2 review, even though it touches a disjoint file (docs/design-system.md). If the review returns findings I resume the 1+2 implementer, and two agents committing in one worktree race on .git/index.lock. Cost if wrong: a few minutes of wall clock per task. Cheap next to a corrupted index mid-batch.
Task 1+2: complete (commits 71e2398..393f3fd, review clean — spec compliant, zero findings at any severity). Reviewer independently verified: every quoted class string byte-identical; both commit trailers exact; 5 files, all inside the fence; the four picker call sites need no edit (prop contract unchanged); no e2e or unit test anywhere asserts the "My Orders" heading text.
Task 3: dispatched (sonnet), BASE 393f3fd
Task 3: implementer DONE, commit 2d7715c. 18 claims corrected, 5 verified-correct-unchanged (Card, Selection, Motion, Gap 4, Tokens block), 0 unverifiable. 1736 tests pass. Implementer flagged two out-of-checklist corrections (Rail/Sheet vocabulary, Input primitive) for reviewer attention. Task reviewer dispatched (sonnet).
Task 3: complete (commit 2d7715c, review clean — 0 Critical, 0 Important). Reviewer independently re-verified 16 claims from source (direction paragraph, three principles, five vocabulary entries, five primitives, hero role, three gap resolutions) plus the price grep (no output) and the fence (one file, one commit, Part 3 heading at line 612 untouched). One Minor: the REPORT cites `src/lib/schema.ts` where the real path is `src/lib/db/schema.ts` — a citation error in the artifact only; the shipped doc text is correct (reviewer re-checked the real file). Not fixed: the report is scratch, not shipped.
Ruling: the implementer's two out-of-checklist corrections (Rail/Sheet vocabulary entries, Input primitive description) STAND as in-scope. The brief's standing method is "if the code contradicts the plan's prediction, the code wins"; both were found false during the pass the brief did assign, both are cited to a file and a verbatim quote, and leaving the Vocabulary Rail entry stale while correcting Principle 3's Rail claim would have made the doc internally inconsistent. The reviewer reached the same conclusion independently. Cost if wrong: two extra corrected paragraphs in a doc whose entire purpose this slice is to make true. Near zero.
Task 4: dispatched (sonnet), BASE 2d7715c
Task 4: implementer DONE, commit 15e31db. 27 claims corrected across 15 sections, 7 sections verified-correct, 0 unverifiable. 1736 tests, price grep clean.
Ruling (Task 4 concern 1, the Pricing-rule renumber): the shift of the Pricing rule's list marker from "6." to "4." STANDS. My constraint was "not a word, not a link, not an issue number" — a list marker is none of the three, and I verified the paragraph's prose is byte-identical in the diff. The alternative is a list numbered 1,2,3,6,5 after two items above it were deleted for naming files that no longer exist; a visibly broken list around the single most-reinstated rule in the repo is worse than a renumber. The implementer also updated the one cross-reference to it ("Home item 4's Pricing rule"), so the doc stays internally consistent. Cost if wrong: a future reader looking for "Home item 6" finds item 4 two lines away. Near zero.
Task 4 reviewer must confirm: the Pricing-rule PROSE is unmodified, and the two out-of-checklist deletions (Proof strip, the /admin list-page Refund claim) are each genuinely absent from the code.
Task 4 review: 1 Important + 2 Minor. All three independently re-verified by me before acting:
 (a) IMPORTANT — `/orders` "Filter tabs — underlined text" is false. `src/app/orders/orders-list.tsx:70-78` uses `border-b-2` ("mirrors the Studio tab strip"); the file's ONLY `underline` is the Track-shipment link at :221. `/admin/page.tsx:52` genuinely does use `underline underline-offset-[3px]`, so the doc gave two structurally different components identical wording. Confirmed.
 (b) MINOR/contradiction — Part 2 vocabulary line 344 "**Sticky bar** — the fixed bottom CTA bar on phone funnel pages (/order)". `/order` is a bare redirect (`src/app/order/page.tsx`); the real sticky checkout bar is `src/app/preview/page.tsx:1295`, which Task 4's own corrected /preview section now documents. Confirmed.
 (c) MINOR — the header's running-jobs badge (`src/components/site-header.tsx:142-150`, `data-testid="running-jobs-badge"`, "N generating", always in the bar not the menu) is absent from Global chrome's inventory. Confirmed.
Ruling: all THREE go into fix round 1, and I LIFT Task 4's "Part 3 only" fence for finding (b). Normally Minors are ledgered and left to the final review, and Parts 1-2 belong to Task 3. But (b) is a direct Part 2/Part 3 contradiction INSIDE ONE PR — shipping a doc that contradicts itself is the exact defect this slice exists to remove — and (c) is a one-line omission in a file the implementer is already opening. Batching them costs one dispatch; deferring (b) ships a self-contradicting doc. Cost if wrong: two extra corrected lines, both source-verified by me personally. Near zero.
Ruling: the reviewer AUDITED and upheld my Pricing-rule renumber ruling, verifying the prose byte-identical (only the leading `6.`->`4.` marker), both cross-references retargeted to "Home item 4", the Footer's to "Home item 5", and zero gaps in any renumbered list. It also independently upheld both out-of-checklist corrections (Proof strip: zero code trace repo-wide; /admin list-page Refund: zero occurrences in admin/page.tsx, present only in admin/orders/[id]/page.tsx:42,116). Both stand.
Task 4: fix round 1/5 dispatched (resume implementer, sonnet), FIX_BASE 15e31db
Task 4: fix round 1/5 (3 addressed, 0 open — orders filter tabs, sticky-bar target, running-jobs badge; commits 15e31db..a8df0dc). Scoped re-review verdict: all addressed, no new breakage; both renumbered lists consecutive (Global chrome 1-5, /orders 1-4), Pricing paragraph in no hunk, trailers exact, price grep clean.
Task 4: complete (commits 2d7715c..a8df0dc, review clean)
All four tasks complete. Running full gate, then Opus whole-branch review.
GATE (whole branch, worktree): lint 0 errors / 21 pre-existing warnings; typecheck clean; 168 files / 1736 tests passed; production build succeeded under the exact CI dummy env from .github/workflows/ci.yml. Opus whole-branch review dispatched.

## Whole-branch review (Opus) — 0 Critical, 7 Important, 7 Minor. Verdict: findings must be fixed first.
The four cross-task seam findings are the class per-task reviews structurally cannot see; I re-verified each from source before acting:
 S1/I3 — doc :472 claims the mono label "fills the role 'section labels'" and the same edit struck section-labels off the text-sm bullet, but SEVEN `block text-sm font-medium mb-2` labels survive, three on live paths (preview/page.tsx:1050 "Product", publish-modal.tsx:96 "Name", background-picker.tsx:64). Task 1 converted two; Task 3 wrote the doc as if it converted all. Confirmed by grep.
 S2/I4 — doc :553 (Gaps item 1) still says badges "collapse to a neutral pill", and THIS BRANCH added two new cross-references pointing at it (:264, :493) while :491 correctly says "no pill/background/radius". The branch shipped a doc contradicting itself across two paragraphs it deliberately linked. Confirmed.
 S3/I7 — the plan (line 180) mandated a TYPOGRAPHY blast-radius check on the four picker call sites; my ledger answered a PROP-CONTRACT question instead ("prop contract unchanged"). Real state: /preview now reads Product (14px sans) -> COLOR — WHITE (11px mono) -> SIZE (11px mono) -> Front/Back (14px sans). The live purchase screen's label vocabulary is now MIXED and nothing recorded it. My miss, not the implementer's.
 S4/I2 — doc :471 says the mono label is "shared as MONO_LABEL"; grep shows exactly 5 importers, ALL under src/app/d/[imageId]/, and twelve other files inline the literal. Acting on "it's shared" would make src/components/ import from src/app/d/ — the exact inversion that module's docblock exists to prevent. Confirmed.
 I1 — doc :469-473 files Badge and EmptyState under the 11px mono label. Both actually use `font-mono uppercase tracking-wide text-[10px]` (badge.tsx:35, empty-state.tsx:38) — different size, different tracking, no leading-4, and EmptyState is text-text-faint. The same edit removed "badges" from the text-[10px] bullet, so the app's SECOND mono label type is now documented nowhere. Confirmed.
 I5 — doc :465 "text-lg/xl semibold — screen title" is false for every live screen: h1 census shows 14 font-bold at text-xl/2xl; the only semibold headings are on retired /dashboard and /shop/[slug]. Confirmed.
 I6 — the committed ledger does not exist yet. Mine to do before the worktree dies (.superpowers/ is git-ignored, .gitignore:57) — exactly the loss CLAUDE.md records from the 2026-09-06 batch.
Ruling (reviewer's challenge to the fence on src/components/published-grid.tsx): the fence STANDS; I am not widening it. The reviewer is right that my own stated rationale — collision with the three named sibling worktrees — does not reach that file, and right that a ledger line in a git-ignored directory is not a record. But the fence is not mine to relax: the controller brief that is this slice's spec says verbatim "**File fence:** ... Nothing else. If (a) reveals a call site that needs a change outside the fence, record it as deferred and leave it." That is the parent session's instruction, and a two-line comment fix is not worth overriding it while two sibling slices are open. I take the reviewer's real point — the RECORD — by making it durable two ways instead: the ledger is committed into docs/superpowers/ledgers/ (inside the fence) and every deferred item is written into the PR body, where the parent merges from. Cost if wrong: published-grid.tsx's docblock keeps telling a future sweep a Shop card shows a price for one more slice. The guard test cannot catch a computed total, so this is real — flagged to the parent as the top deferred item.
Ruling (reviewer's challenge to R4's cost-if-wrong): CONCEDED, and it changes the deferred list, not the code. I priced the /orders heading edit and missed the seam it opened: /studio (studio/layout.tsx:22, text-xl sm:text-2xl font-bold) is the target of /orders' own "New Design" button, so the branch now puts an 11px mono masthead one tap from a 24px bold one. My deferred list caught /cart and missed /studio, the larger half. The h1 census (14 bold vs 2 mono mastheads) goes in the PR body so the #188 slice that owns page headings inherits the real question — is the mono masthead the house treatment for every top-level page, or only for grid/list pages — rather than a one-page anomaly. No code change here: /studio is a sibling worktree's file and genuinely fenced.
Ruling (reviewer's challenge to R8's execution): CONCEDED. The pattern it names is real — Part 2's prose was corrected while the LISTS were corrected selectively, and all three surviving falsehoods (I1, I3, I5) plus gap 1 (I4) are list items. Fixing all four in the single fix wave.
Fix wave (whole-branch): dispatching ONE implementer with Importants 1-5 + Minors 8, 9, 12. I6 (commit the ledger) and I7 (record the /preview mixed-label state) are mine. Minors 10, 11, 13, 14 -> PR body / deferred.
Final fix wave: commit 569c973, all 8 findings (I1-I5, M8/M9/M12) fixed in the two fenced files. Checks: two fenced files only, price grep clean, no-preselection-price 3/3, orders-list 8/8, Pricing paragraph in no hunk.
Ruling: the implementer CORRECTED MY OWN COUNTS in the F2 dispatch and was right to. I told it MONO_LABEL had 5 importers and 12 inliners; grep shows 4 and 11 — my `grep -rln "mono-label"` matched a COMMENT in d/[imageId]/page.tsx ("Title/attribution as one mono-labelled block"), not an import. I re-verified: page.tsx neither imports nor inlines it. It wrote what grep showed and flagged the divergence, which is exactly the "code wins" behaviour the dispatch asked for. Cost if wrong: none — the shipped number is the true one and my briefing error did not reach the doc.
Final fix wave re-review (haiku, scoped): all 8 findings ADDRESSED, no new breakage. Two fenced files only, price grep clean, orders-list 8/8, no-preselection-price 3/3, Pricing paragraph in no hunk, no gap renumbered, trailers exact.
FINAL GATE (whole branch, worktree, head 569c973): lint 0 errors / 21 pre-existing warnings; typecheck clean; 168 files / 1736 tests passed; production build compiled successfully under the exact CI dummy env.

## Deferred (out of fence — for the parent and the PR body)
1. `src/components/published-grid.tsx:19-21` — docblock still says a Shop card shows "then what it costs and on what garment", describing the price line PR #226 deleted. The Opus reviewer rated this MUST-FIX-BEFORE-MERGE because the guard test greps for `From $` and three helper names and therefore CANNOT catch a sweep re-adding a *computed* total, and because this branch's own new /shop section now says the opposite. Left per the brief's explicit "Nothing else / record it as deferred and leave it". HIGHEST-VALUE deferred item.
2. `src/app/cart/page.tsx:97` — `<h1 className="text-xl sm:text-2xl font-bold mb-6">Your cart</h1>`, the pre-Paper display heading this branch removes from /orders.
3. `src/app/studio/layout.tsx:22` — same, and it is the target of /orders' own "New Design" button, so the branch now puts an 11px mono masthead one tap from a 24px bold one. The h1 census is 14 bold vs 2 mono mastheads (/shop, /orders): the open design question is whether the mono masthead is the house treatment for every top-level page or only for grid/list pages. That is a #188 call, not a leftover.
4. `/preview`'s label vocabulary is now MIXED by this branch: Product (14px sans, preview/page.tsx:1050) sits beside COLOR/SIZE (11px mono), with Front/Back (14px sans, :1097/:1117) below. Retired-path equivalent: compose-form.tsx:153,197,218,261. No code change wanted — /preview is a sibling worktree's file — but the state must be recorded, which the doc now does.
5. `src/components/ui/button.tsx:5-7` docblock says the `generate` variant is "wired in a later slice"; it is wired now (studio-client.tsx:758, maker-hero.tsx:57). Same shape as item 1, out of fence (ui/* frozen).
6. `src/app/order/confirm/page.tsx:53` — "Your order is listed in My Orders." now names a page titled ORDERS. R5 covered the two buttons, not this sentence.
7. `/admin/published`'s h1 keeps `text-xl font-bold` (matches /admin and /admin/errors) — a deliberate choice, recorded here because nothing else records it. The radius and border-side changes there have no assertion: it is an async server component behind an auth redirect with no existing harness.
