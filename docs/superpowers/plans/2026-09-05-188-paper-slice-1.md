# Plan: Paper look, rollout slice 1 — tokens + primitives + hardcoded-dark sweep (#188)

**Spec authority:** `docs/ux-design-review-2026-09.md` — "Shared components
audit", "Hardcoded darks that must be swept", "Rollout plan" item 1 — and
Nico's decisions recorded there and in CLAUDE.md (2026-09-04/05): Look B
"Paper", variant **PaperB "quieter"**, **LIGHT ONLY**, rose **"One Mark"**
(rose wordmark + solid rose Generate; everything else ink). Persona stays The
Clean Label (`docs/design-system.md` Part 1). **Branch:** this one
(`cloud/188-paper-slice-1`). Migration-free. **Slice 1 only** — the nav
model (slice 2), the Studio bench layout (#187 points 1/3), and the mocked
screens are NOT in this PR.

## Tokens (PaperB, light only)

- Ground: warm off-white `oklch(0.97 0.008 80)`. Ink `#141311`. Muted/faint
  text as two steps toward the ground, not grey-on-black inverted.
- Borders: 1px ink (or ink at ~25% for hairlines). **No shadows anywhere.**
- Primary button: outlined ink, ink text (not filled). Links underlined.
  One mono label style (uppercase, tracking) for small labels/markers.
- Rose: ONE accent token (`--accent-rose`, pick a rose that passes AA on the
  ground for text and on white for the wordmark; document the value). Used
  for exactly two things in slice 1: the wordmark and the solid Generate
  button (the Studio composer's submit, the landing hero's Generate). Nothing
  else is rose. A fuller rose palette may come later as a token-only change —
  keep every colour behind a token so that swap is one file.
- `--positive`/`--negative` recoloured for a light ground (they are
  dark-theme `#4ade80`/`#f87171` today).
- There is no dark-mode mechanism to remove — only tokens. Do not add a
  `prefers-color-scheme` branch.

## Sweep (from the review doc's list — every item, file:line there)

- `src/app/globals.css` tokens; `.bg-checkerboard` → decide per the doc's Q2:
  on paper the checkerboard is noise. Replace the working-view ground with a
  plain bordered paper well (`--surface-well`), keeping the class name as an
  alias so the 14 call sites don't all change in this PR (they may).
- `src/lib/instant-preview.ts` `isDarkShirt` and `mockupBackdrop`: re-decide
  on a warm ground — the mockup `mix-blend-multiply` needs a LIGHT backdrop;
  use the page ground for light shirts and a light neutral for dark shirts.
  Consumers listed in the doc.
- `bg-gray-900` wells, `bg-black/*` scrims and pills, the 15 `text-white`
  literals, header dropdown shadow → 1px border. Modal/lightbox/drawer scrim →
  ink at ~20%.
- Primitives `src/components/ui/{button,input,card,badge,modal,quick-reply}.tsx`
  + `breadcrumbs.tsx` re-skinned per the audit (disabled = dotted border +
  muted text, not opacity-30; Badge = mono text, colour only for
  shipped/canceled).
- Three inputs bypassing `Input` (`maker-hero.tsx`, `studio-client.tsx`,
  `chat-panel.tsx` ×2) routed through it.
- ONE `EmptyState` component replacing the nine near-identical blocks; CTA
  targets unchanged in this slice (retargeting to `/studio` is slice 2).
- `src/lib/email.ts` constants aligned to the Paper warm ground; OG cards
  (`src/lib/og-site-card.tsx`) → paper ground, ink wordmark in rose; the
  per-image share card keeps artwork-on-backdrop. OG raster cannot be
  unit-tested (satori/resvg under vitest) — verify with `next build && next
  start` and fetch the three routes, as PR #175 did; say what you saw.
- `layout.tsx` metadata title/description still mention phone cases — fix
  the copy (Clean Label) while you are there; add a simple `src/app/icon.svg`
  (rose mark on paper) since none exists.
- `docs/design-system.md`: tokens section rewritten light-first; the
  "ink/paper" language it already uses is now literal.

## Global constraints

- Phone-first; verify the 390px composer, header, and `/preview` sticky bar
  in your own screenshots (Playwright against `npm run dev`/`next start`
  locally — no prntd.org or Vercel access from a cloud session).
- Contrast: every text/ground pair AA. Put the computed ratios in the PR
  body for ink, muted, faint, rose-on-ground, rose-on-white.
- Do not change layout, copy (beyond the metadata fix), routes, or nav.
  Large diff, low logic — a reviewer must be able to say "same page, new
  skin" for every file.
- e2e specs assert on `data-testid`s, not colours; they should stay green.
  `npm run lint`, `typecheck`, `test`, `build` green.
- Expect merge conflicts with in-flight PRs touching `studio-client.tsx`,
  `library-grid.tsx`, `buy-hero.tsx`, `preview/page.tsx`: rebase onto
  `origin/main` before opening the PR and again before requesting merge.

## Tasks

Each task is one commit (or a few). Every task ends with `npm run lint`,
`npm run typecheck`, `npm test` green; Task 3 and later also `npm run build`
(under the CI dummy env block from `.github/workflows/ci.yml` if no
`.env.local` is present). The review doc's file:line references were
written 2026-09-04 and several files have moved since (#196, #199, #200,
#203, #205) — grep for the pattern, do not trust the line numbers.

## Task 1 — tokens, `globals.css`, `design-system.md`

`src/app/globals.css` gets the PaperB token set from the Tokens section
above (ground, ink, muted/faint as two steps toward the ground, border =
ink at ~25% for hairlines / ink for 1px rules, `--accent-rose`, recoloured
`--positive`/`--negative`, `--surface-well`). Keep every existing token
NAME so no call site breaks; add the new ones. Delete no token. Do not add
a `prefers-color-scheme` branch. `.bg-checkerboard` becomes an alias for
the paper well (plain `--surface-well` fill + 1px hairline border) — the
class name stays so the 14 call sites keep compiling. Rewrite the tokens
section of `docs/design-system.md` light-first with the exact values and
the computed AA contrast ratios for ink / muted / faint on the ground and
rose on the ground + on white (compute them; cite the numbers). Build must
be green; no component files change in this task. Expect the app to look
half-done after this commit — that is the plan's accepted risk.

## Task 2 — primitives, breadcrumbs, `EmptyState`, bypassing inputs

Re-skin `src/components/ui/{button,input,card,badge,modal,quick-reply}.tsx`
and `src/components/breadcrumbs.tsx` per the review doc's "Shared
components audit": Button `primary` = outlined ink with ink text (NOT
filled), a new `variant="generate"` (or equivalent prop) = solid rose fill
for the two Generate buttons only, `secondary` = lighter outline or
underlined text, `danger` keeps `--negative` on hover, disabled = dotted
border + muted text (no `opacity-30`); Input = paper fill, 1px ink border,
ink focus ring; Card = paper with border; Badge = mono uppercase text, no
pill, colour only for shipped/canceled via the two status tokens; Modal
scrim = ink at ~20%; QuickReply = outlined ink pill, 44px targets;
Breadcrumbs links underlined, mono separator. Route the three inputs that
bypass `Input` (`maker-hero.tsx`, `studio-client.tsx`, `chat-panel.tsx` ×2)
through it. Create ONE `src/components/ui/empty-state.tsx` (label, one
line, one link/button) and replace the nine near-identical `text-center
py-16` blocks the doc lists (grep `py-16` to find them all) — CTA targets
unchanged in this slice. Existing primitive tests in
`src/components/ui/__tests__/` updated, plus a test for `EmptyState`.

## Task 3 — the hardcoded-dark sweep, `instant-preview`, checkerboard sites

Every item under "Hardcoded darks that must be swept": `bg-gray-900`
wells, `bg-black/*` scrims and pills, every `text-white` literal, the
header dropdown shadow → 1px border, lightbox/drawer/stage scrims → the
Modal's ink scrim. Grep for each pattern across `src/` and leave zero
matches outside tests (state the final grep counts in the report).
`src/lib/instant-preview.ts`: re-decide `isDarkShirt` and `mockupBackdrop`
for a warm ground — the mockup `mix-blend-multiply` needs a LIGHT backdrop;
use the page ground token value for light shirts and a light neutral for
dark shirts; update its unit tests and the consumers the doc lists.
Rose: wire the wordmark in `site-header.tsx` and the two Generate buttons
(Studio composer submit, landing `maker-hero.tsx` Generate) to the rose
token; nothing else is rose. `npm run build` green.

## Task 4 — email, OG, icon, metadata

`src/lib/email.ts` colour constants → the Paper warm ground / ink (same
values as `globals.css`); `src/lib/og-site-card.tsx` → paper ground, ink
text, wordmark in rose (the per-image share card keeps artwork-on-backdrop;
`designCardPalette` unchanged); add `src/app/icon.svg` (rose mark on paper,
simple); `src/app/layout.tsx` metadata title/description rewritten to
Clean Label copy with no phone cases (e.g. title "PRNTD", description
"Design a shirt by describing it."). OG raster cannot be unit-tested —
verify with `npm run build && npx next start -p 3100` (env: if
`.env.local` is absent, run with the CI dummy env block) and fetch
`/opengraph-image`, `/twitter-image`, and one `/d/<id>/opengraph-image`;
save the PNGs into the SDD workspace and say what you saw. Email template
snapshot tests updated.

## Task 5 — screenshots + contrast table for the PR body

Playwright screenshots at 390 and 1440 wide for `/`, `/studio` (empty +
with lanes), `/design?id=`, `/d/[imageId]`, `/preview`, `/orders`,
`/sign-in`, against a compiled local build (`npm run build && node
--env-file=<path given by the controller> node_modules/.bin/next start -p
3100`; the dev DB has seeded designs — read `scripts/seed-dev-db.ts` and
`e2e/helpers/auth.ts` for how to get a signed-in session). Save under the
SDD workspace `screenshots/` and write `pr-body.md` there: the contrast
table from Task 1, the screenshot list, the grep counts from Task 3, and
what the OG fetches showed. No code changes in this task; if a screenshot
reveals a visual defect, report it as a concern with the file — do not fix
it here.

Use superpowers:subagent-driven-development to execute; the final
whole-branch review must be on the most capable model and check the
"same page, new skin" property file by file. Open a normal PR; merge on
green CI + clean final review (Nico's ruling 2026-09-06: "merge, I trust
you").
